def send_sms(message, number="CLINIC_NUMBER"):
    # placeholder: replace with real SIM800L / GSM sending code
    print(f"[SMS] To {number}: {message}")

def unlock_drawer_for(injury_key):
    # placeholder: replace with GPIO solenoid trigger
    print(f"[SOLENOID] Unlock drawer for: {injury_key}")